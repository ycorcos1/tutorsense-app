# TutorSense — System Architecture

**Visual representation of data flow and system components**

---

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         TUTORSENSE SYSTEM                        │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│  Data Layer  │
└──────────────┘
     ↓
┌──────────────────────────────────────┐
│  /data/tutors.csv                    │
│  /data/sessions.csv                  │
│  - 14 days historical data           │
│  - 200 tutors, ~3,000 sessions       │
└──────────────────────────────────────┘
     ↓
┌──────────────┐
│ Batch Layer  │
└──────────────┘
     ↓
┌──────────────────────────────────────┐
│  scripts/score_tutors.ts             │
│  ┌────────────────────────────────┐  │
│  │ 1. Parse CSVs                  │  │
│  │ 2. Aggregate KPIs per tutor    │  │
│  │ 3. Calculate scores (0-100)    │  │
│  │ 4. Identify at-risk tutors     │  │
│  │ 5. Generate AI explanations    │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
     ↓
┌──────────────────────────────────────┐
│  /public/scores.json                 │
│  /public/explanations.json           │
└──────────────────────────────────────┘
     ↓
┌──────────────┐
│   API Layer  │
└──────────────┘
     ↓
┌──────────────────────────────────────┐
│  GET  /api/scores                    │
│  GET  /api/tutor/[id]                │
│  POST /api/recompute                 │
└──────────────────────────────────────┘
     ↓
┌──────────────┐
│  UI Layer    │
└──────────────┘
     ↓
┌──────────────────────────────────────┐
│  Dashboard (app/page.tsx)            │
│  ┌────────────────────────────────┐  │
│  │ Header                         │  │
│  │ - Logo                         │  │
│  │ - Last Updated                 │  │
│  │ - Filters                      │  │
│  ├────────────────────────────────┤  │
│  │ AtRiskTable Component          │  │
│  │ - Tutor list                   │  │
│  │ - Score chips                  │  │
│  │ - Sparklines                   │  │
│  ├────────────────────────────────┤  │
│  │ TutorDrawer Component          │  │
│  │ - Detailed KPIs                │  │
│  │ - Full explanation             │  │
│  │ - Suggested actions            │  │
│  ├────────────────────────────────┤  │
│  │ Footer                         │  │
│  │ - Cost estimate                │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
┌─────────────┐
│   START     │
└─────────────┘
      ↓
┌─────────────────────────────────────┐
│ 1. Generate Synthetic Data          │
│    (scripts/generate_synthetic_data) │
│    - 200 tutors                      │
│    - 3,000 sessions over 14 days    │
└─────────────────────────────────────┘
      ↓
┌─────────────────────────────────────┐
│ 2. Score Tutors                      │
│    (scripts/score_tutors.ts)         │
│                                      │
│    For each tutor:                   │
│    ├─ Aggregate sessions             │
│    ├─ Calculate KPIs                 │
│    ├─ Compute score                  │
│    └─ Build 7-day trend              │
└─────────────────────────────────────┘
      ↓
┌─────────────────────────────────────┐
│ 3. Identify At-Risk                  │
│    - Score < 60 OR bottom 20%        │
│    - Max 50 tutors                   │
└─────────────────────────────────────┘
      ↓
┌─────────────────────────────────────┐
│ 4. Generate Explanations             │
│    ┌───────────────────────────────┐ │
│    │ Has API Key?                  │ │
│    ├─── YES → OpenAI/OpenRouter    │ │
│    └─── NO  → Fallback templates   │ │
└─────────────────────────────────────┘
      ↓
┌─────────────────────────────────────┐
│ 5. Write Output Files                │
│    ├─ /public/scores.json            │
│    └─ /public/explanations.json      │
└─────────────────────────────────────┘
      ↓
┌─────────────────────────────────────┐
│ 6. API Serves Data                   │
│    ├─ Merge scores + explanations    │
│    └─ Return to frontend             │
└─────────────────────────────────────┘
      ↓
┌─────────────────────────────────────┐
│ 7. Dashboard Displays                │
│    ├─ Filter by subject/score        │
│    ├─ Color-coded chips              │
│    ├─ Sparklines for trends          │
│    └─ Drawer for details             │
└─────────────────────────────────────┘
```

---

## ⚙️ Scoring Pipeline Detail

```
INPUT: sessions.csv
─────────────────────────────────────────

┌───────────────────────────────────────┐
│ Parse & Group by Tutor                │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Calculate Per-Tutor KPIs              │
│                                       │
│ dropout_rate = dropouts / total       │
│ issue_rate = issues / total           │
│ reschedule_rate = reschedules / total │
│ avg_rating = sum(ratings) / count     │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Apply Scoring Formula                 │
│                                       │
│ score = 100 - (                       │
│   dropout_rate × 40 +                 │
│   issue_rate × 30 +                   │
│   reschedule_rate × 20 +              │
│   (5 - avg_rating) × 10               │
│ )                                     │
│                                       │
│ clamp to [0, 100]                     │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Calculate 7-Day Rolling Trend         │
│                                       │
│ For each of last 7 days:              │
│   - Compute daily score               │
│   - Store in trend array              │
└───────────────────────────────────────┘
      ↓
OUTPUT: Tutor score object
```

---

## 🤖 AI Explanation Pipeline

```
INPUT: At-risk tutor scores
─────────────────────────────────────────

┌───────────────────────────────────────┐
│ Filter Tutors                         │
│ - Score < 60 OR bottom 20%            │
│ - Limit to 50 max                     │
└───────────────────────────────────────┘
      ↓
For each at-risk tutor:
      ↓
┌───────────────────────────────────────┐
│ Build Prompt                          │
│                                       │
│ "Tutor: {name}                        │
│  Score: {score}                       │
│  KPIs:                                │
│    - Avg Rating: {rating}             │
│    - Dropouts: {dropout}%             │
│    - Tech Issues: {issues}%           │
│    - Reschedules: {reschedules}%      │
│                                       │
│  Generate:                            │
│  1. One-line explanation why at-risk  │
│  2. Suggested coaching action"        │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Call LLM API                          │
│ ┌───────────────────────────────────┐ │
│ │ Success?                          │ │
│ ├─── YES → Use AI response          │ │
│ └─── NO  → Use template:            │ │
│                                       │
│      "Low score due to {top_issues}   │
│       Suggest: {relevant_coaching}"   │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Track Metrics                         │
│ - Tokens used                         │
│ - Cost estimate                       │
│ - Generation time                     │
└───────────────────────────────────────┘
      ↓
OUTPUT: Explanation object
```

---

## 🔐 API Authentication Flow

```
POST /api/recompute
─────────────────────────────────────────

┌───────────────────────────────────────┐
│ 1. Receive Request                    │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ 2. Extract Authorization Header       │
│    Expected: "Bearer {secret}"        │
└───────────────────────────────────────┘
      ↓
      ┌─────────────┐
      │ Header      │
      │ exists?     │
      └─────────────┘
        ↓         ↓
      NO         YES
       ↓          ↓
   ┌──────┐   ┌────────────────────┐
   │ 401  │   │ 3. Compare Secret  │
   │ Exit │   │    with env var    │
   └──────┘   └────────────────────┘
                     ↓
              ┌─────────────┐
              │ Matches?    │
              └─────────────┘
                ↓         ↓
              NO         YES
               ↓          ↓
           ┌──────┐   ┌────────────────────┐
           │ 401  │   │ 4. Run scoring     │
           │ Exit │   │    pipeline        │
           └──────┘   └────────────────────┘
                           ↓
                   ┌────────────────────┐
                   │ 5. Return summary  │
                   │    JSON (200)      │
                   └────────────────────┘
```

---

## 🕐 Automated Cron Flow

```
Vercel Cron (Daily at 02:00 UTC)
─────────────────────────────────────────

┌───────────────────────────────────────┐
│ Trigger: vercel.json cron config      │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ POST /api/recompute                   │
│ (with internal Vercel auth)           │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Execute score_tutors.ts               │
│ ├─ Read /data/*.csv                   │
│ ├─ Calculate scores                   │
│ ├─ Generate explanations              │
│ └─ Write to /public/*.json            │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Log Results                           │
│ - Duration                            │
│ - Tutors processed                    │
│ - Explanations generated              │
│ - Any errors                          │
└───────────────────────────────────────┘
      ↓
┌───────────────────────────────────────┐
│ Next Dashboard Load                   │
│ → Shows fresh data automatically      │
└───────────────────────────────────────┘
```

---

## 🎨 Frontend Component Hierarchy

```
app/page.tsx (Dashboard)
│
├─ Header
│  ├─ Logo
│  ├─ LastUpdated timestamp
│  ├─ DatePicker (UI-only)
│  └─ RefreshButton
│
├─ Filters
│  ├─ SubjectDropdown
│  │  └─ Options: All, Math, Science, English, etc.
│  └─ ScoreThresholdSlider
│     └─ Range: 0-100, default: 60
│
├─ AtRiskTable
│  └─ For each tutor:
│     ├─ Name
│     ├─ Subject
│     ├─ ScoreChip (color-coded)
│     ├─ WhyExplanation (one-line)
│     └─ Sparkline (7-day trend)
│
├─ TutorDrawer (opens on row click)
│  ├─ Header
│  │  ├─ Tutor name
│  │  └─ Close button
│  ├─ KPI Cards
│  │  ├─ Avg Rating
│  │  ├─ Dropout %
│  │  ├─ Tech Issue %
│  │  ├─ Reschedule %
│  │  └─ Sessions Count
│  ├─ TrendChart (7-day detailed)
│  └─ ExplanationSection
│     ├─ "Why" explanation
│     └─ "Suggested Action"
│
└─ Footer
   └─ CostEstimate
      └─ "Est. Daily Cost: $X.XX"
```

---

## 📦 File Dependencies

```
generate_synthetic_data.ts
├─ Dependencies: faker (or similar)
└─ Outputs: /data/tutors.csv, /data/sessions.csv

score_tutors.ts
├─ Dependencies: papaparse, openai, zod
├─ Inputs: /data/tutors.csv, /data/sessions.csv
├─ Environment: OPENAI_API_KEY or OPENROUTER_API_KEY
└─ Outputs: /public/scores.json, /public/explanations.json

/app/api/scores/route.ts
├─ Dependencies: None (reads static files)
├─ Inputs: /public/scores.json, /public/explanations.json
└─ Returns: Merged JSON response

/app/api/tutor/[id]/route.ts
├─ Dependencies: None
├─ Inputs: /public/scores.json
└─ Returns: Single tutor time-series

/app/api/recompute/route.ts
├─ Dependencies: Child process to run score_tutors.ts
├─ Environment: RECOMPUTE_SECRET
├─ Triggers: score_tutors.ts execution
└─ Returns: Summary JSON

/app/page.tsx
├─ Dependencies: React, components
├─ API Calls: GET /api/scores
└─ Renders: Dashboard UI
```

---

## 🔄 State Management

```
┌────────────────────────────────────────┐
│ Frontend State (React)                 │
├────────────────────────────────────────┤
│ tutors: TutorScore[]                   │
│ filteredTutors: TutorScore[]           │
│ selectedTutor: TutorScore | null       │
│ subjectFilter: string                  │
│ scoreThreshold: number                 │
│ loading: boolean                       │
│ error: string | null                   │
│ lastUpdated: Date                      │
└────────────────────────────────────────┘
       ↑                    ↓
       │                    │
  ┌────┴────┐         ┌────┴────┐
  │  Fetch  │         │ User    │
  │  /api/  │         │ Actions │
  │  scores │         │         │
  └─────────┘         └─────────┘
```

---

## 💾 Data Storage Locations

```
Development:
├─ /data/                     (Git committed)
│  ├─ tutors.csv
│  └─ sessions.csv
├─ /public/                   (Git committed)
│  ├─ scores.json
│  └─ explanations.json
└─ .env.local                 (Git ignored)

Production (Vercel):
├─ /data/                     (Deployed from Git)
├─ /public/                   (Deployed from Git, updated by cron)
└─ Environment Variables      (Vercel dashboard)
```

---

## 🧪 Testing Strategy

```
Unit Tests
├─ Scoring formula
├─ KPI aggregation
├─ Score clamping
└─ Edge case handling

Integration Tests
├─ CSV parsing
├─ API endpoints
├─ Authentication
└─ Error handling

Performance Tests
├─ 3,000 sessions < 10 min
├─ API response < 500ms
└─ Dashboard render < 2s

End-to-End Tests
├─ Generate → Score → View
└─ Manual recompute flow
```

---

**Last Updated:** November 6, 2025  
**Version:** 1.0
